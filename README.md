# BastetTetlegram 🐈✨

**BastetTetlegram** — это не просто Telegram-бот, это ваш личный **AI-советник, генератор радости и хранитель таинственных "дней без соседей"** 📊. Написан на Go, работает на OpenAI и, возможно, немного кото-магии 🐾. Заведует этим зоопарком искусственный интеллект, который готов отвечать на вопросы, создавать миры и даже тосты 🥂.

## Особенности 🌟

- **Интеграция с OpenAI** 🧠: Использует мощные модели `gpt-4o` и `gpt-4o-mini` для генерации текста и `dall-e-3` для создания изображений.
- **Генерация текста** ✍️: Отвечает на вопросы, пишет рассказы, генерирует идеи и многое другое по команде `/gpt`. Удивительно умен, но не гарантирует, что не спутает кота с собакой 🐶🐱.
- **Генерация изображений** 🎨: Создает визуальные шедевры по вашему описанию с помощью команды `/imagine`. Внимание: результаты могут быть неожиданными 😱.
- **Случайные цитаты** 💬: Отправляет мудрую (или не очень) цитату из файла по команде `/q`. Вдохновляет, или заставляет задуматься 🤔.
- **Случайные тосты** 🥂: Порадует вас случайным тостом с праздничным эмодзи по команде `/toast`. Пьём за науку! 🍷
- **Счетчик "дней без соседей"** 📊: Следит за временем с момента последнего упоминания слова "сосед" в чате, сохраняя прогресс даже после перезапуска бота. Это важная статистика! 📈
- **Команды помощи** ❓: Команды `/start` и `/help` предоставят вам необходимую информацию (если бот не уснул 🛌).
- **Управление сообщениями** 🧹: Команда `/me` удаляет вызвавшее её сообщение. Полезно для анонимных признаний или сожжения улик 🔥.
- **Надежность** 🔧: Обработка ошибок, таймауты для запросов к API, логирование и (в будущем!) тесты 🧪.

## Установка и запуск 🚀

1.  **Требования** 📋:
    - Установленный [Go](https://golang.org/dl/) (версия 1.20 или выше).
    - Аккаунт [Telegram](https://telegram.org/) и [токен бота](https://core.telegram.org/bots#how-do-i-create-a-bot) от [@BotFather](https://t.me/BotFather).
    - Аккаунт [OpenAI](https://platform.openai.com/signup) и [API ключ](https://platform.openai.com/api-keys).

2.  **Настройка конфигурации** 🔐:
    - Создайте пакет `internal/config` (или используйте `configs/config.yaml` и библиотеку `viper`).
    - Убедитесь, что у вас есть способ загрузки `TELEGRAM_BOT_TOKEN` и `OPENAI_API_KEY`, например, из переменных окружения или файла конфигурации.
    - **НИКОГДА** не коммитьте токены в репозиторий! 🙀

3.  **Подготовка файлов** 📁:
    - Создайте папку `config` в корне проекта.
    - Внутри папки `config` создайте файл `phrases.txt`. Поместите в него цитаты, каждая на новой строке.
    - Внутри папки `config` создайте файл `toasts.txt`. Поместите в него тосты, разделенные `* * *`.

4.  **Клонирование репозитория** 🧬:
    ```bash
    git clone https://github.com/ВАШ_АККАУНТ/BastetTetlegram.git
    cd BastetTetlegram
    ```

5.  **Установка зависимостей** 📦:
    ```bash
    go mod tidy
    ```

6.  **Запуск бота** ▶️:
    ```bash
    go run ./cmd/bot/main.go
    ```

    Бот начнет работу и будет слушать обновления от Telegram. Убедитесь, что ваш сервер/машина, на которой запущен бот, имеет доступ в интернет.

## Использование 📲

После запуска и добавления бота в Telegram-чат, вы можете использовать следующие команды:

- `/start` 🎉: Отправляет приветственное сообщение с кратким описанием возможностей.
- `/help` ❓: Показывает список доступных команд и их описание.
- `/gpt <ваш запрос>` 🧠: Отправляет ваш запрос в модель OpenAI GPT и возвращает сгенерированный ответ. Например, `/gpt Расскажи анекдот`.
- `/imagine <описание изображения>` 🖼️: Запрашивает у OpenAI создание изображения на основе вашего описания. Например, `/imagine Кот в космосе`.
- `/q` 💬: Получает и отправляет случайную цитату из файла `config/phrases.txt`.
- `/toast` 🥂: Получает и отправляет случайный тост из файла `config/toasts.txt`, украшенный случайным праздничным эмодзи.
- `/me` 🧹: Удаляет сообщение, в котором была вызвана команда.

Также бот автоматически отслеживает упоминания слова "сосед" в чате и сообщает, сколько дней прошло с момента последнего упоминания, сохраняя этот счетчик между перезапусками. Это важно! 📈

## Структура проекта 🗂️
```bash
BastetTetlegram/
├── cmd/
│   └── bot/
│       └── main.go          # Основная точка входа
├── internal/
│   ├── bot/                 # Логика бота, обработчики команд
│   │   ├── bot.go
│   │   └── handlers/        # Обработчики команд
│   │       ├── me.go
│   │       ├── gpt.go
│   │       ├── imagine.go
│   │       ├── start.go
│   │       ├── help.go
│   │       ├── quote.go
│   │       ├── toast.go
│   │       └── iddqd.go
│   ├── config/              # Конфигурация приложения
│   │   ├── config.go
│   │   └── load.go
│   ├── files/               # Работа с файлами
│   │   └── files.go
│   ├── models/              # Структуры данных
│   │   └── models.go
│   └── services/            # Бизнес-логика (OpenAI, генераторы)
│       ├── openai.go
│       └── generator.go
├── configs/                 # Файлы конфигурации (yaml, env)
├── config/                  # Файлы с данными (phrases.txt, toasts.txt)
├── go.mod
├── go.sum
└── README.md
```
Описание содержимого:
```bash
cmd/bot/main.go: Точка входа в приложение. Загружает конфигурацию и запускает бота.
internal/bot/bot.go: Основная логика бота, запуск цикла получения обновлений.
internal/bot/handlers/: Пакеты, содержащие логику для каждой команды (/start, /help, /gpt и т.д.).
internal/config/: Определение структуры конфигурации и логика её загрузки.
internal/files/: Функции для чтения/записи файлов (цитаты, тосты, время).
internal/models/: Структуры данных, используемые в приложении.
internal/services/: Бизнес-логика, например, взаимодействие с OpenAI API, генерация случайных элементов.
configs/: Место для файлов конфигурации приложения (например, config.yaml).
config/: Место для файлов с данными, используемыми ботом (например, phrases.txt, toasts.txt).
go.mod, go.sum: Файлы управления зависимостями Go.
```
## Зависимости 📚

- `github.com/go-telegram-bot-api/telegram-bot-api/v5`: Для взаимодействия с Telegram Bot API.
- `github.com/sashabaranov/go-openai`: Для взаимодействия с OpenAI API.
- `github.com/spf13/viper` (опционально): Для загрузки конфигурации.

## Важно 🚨

- Храните свои токены (`Token`, `GPTtoken`) в секрете. Используйте `.env` файлы и переменные окружения в продакшене.
- Убедитесь, что вы соблюдаете [условия использования OpenAI](https://openai.com/policies/terms-of-use) и [правила использования Telegram Bot API](https://core.telegram.org/bots/api#terms-of-service).
- Файл `last_mention.json` будет автоматически создан в директории запуска бота для хранения времени последнего упоминания "сосед".
- Этот бот может вызывать приступы веселья, внезапное вдохновение или философские размышления. Авторы не несут ответственности за последствия. 😄
